apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parsing.fullname" . }}-test-connection"
  labels:
    {{- include "parsing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parsing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
